{
  "name": "near-contract-verifier-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome/Firefox extension that verifies NEAR contract accounts from NEAR RPC.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint:manifest": "node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.log('manifest ok')\"",
    "verify": "npm run lint:manifest && npm test"
  },
  "keywords": ["near", "browser-extension", "contract-verifier"],
  "license": "MIT"
}
